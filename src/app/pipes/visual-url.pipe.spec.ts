import { VisualUrlPipe } from './visual-url.pipe';

describe('VisualUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new VisualUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
